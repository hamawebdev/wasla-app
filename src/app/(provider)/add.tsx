// Phantom route — tab intercepted by WaslaTabBar to open the provider action sheet.
// This file must exist so expo-router resolves the route without warnings.
import React from 'react';
import { View } from 'react-native';

export default function AddPlaceholder() {
  return <View />;
}
